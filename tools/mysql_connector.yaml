identity:
  name: mysql_connector
  author: bion_wang
  label:
    en_US: mysql_connector
    zh_Hans: mysql_connector
    pt_BR: mysql_connector
description:
  human:
    en_US: A tool for connect and querying a mySQL database.
    zh_Hans: A tool for connect and querying a mySQL database.
    pt_BR: A tool for connect and querying a mySQL database.
  llm: A tool for connect and querying a mySQL database.
parameters:
  - name: query
    type: string
    required: true
    label:
      en_US: Query string
      zh_Hans: 查询语句
      pt_BR: Query string
    human_description:
      en_US: A tool for connect and querying a mySQL database.
      zh_Hans: A tool for connect and querying a mySQL database.
      pt_BR: A tool for connect and querying a mySQL database.
    llm_description: A tool for connect and querying a mySQL database.
    form: llm
extra:
  python:
    source: tools/mysql_connector.py
